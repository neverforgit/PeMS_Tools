All executable scripts should be placed here.

To make the relative imports of the local modules work, you need to run using the -m switch
from the parent directory:

*\PeMS_scraper>python -m bin.test